
void loadExperiment(const char* readPtr);
void loadExperiment(const unsigned char* readPtr);
void loadExperiment(const short* readPtr);
void loadExperiment(const unsigned short* readPtr);
void loadExperiment(const int* readPtr);
void loadExperiment(const unsigned int* readPtr);
void loadExperiment(const int64_t* readPtr);
void loadExperiment(const uint64_t* readPtr);
void loadExperiment(const float* readPtr);
